# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'MagicLick' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MagicLick
  pod 'Beethoven'
  pod 'Hue', '~> 3.0'
  pod 'Cartography'

  target 'MagicLickTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MagicLickUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
